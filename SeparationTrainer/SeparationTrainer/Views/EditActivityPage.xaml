<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SeparationTrainer.Views.EditActivityPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Command="{Binding CancelCommand}"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout Padding="15">
            <Label Text="Stress Level"/>
            <Picker ItemsSource="{Binding StressLevels}"
                    SelectedItem="{Binding SelectedStressLevel}"/>

            <Label Text="Notes"/>
            <Entry Text="{Binding ActivityToEdit.Notes}" 
                   Placeholder="Add Notes Here"/>

            <Button Text="Save" Command="{Binding UpdateActivityCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>